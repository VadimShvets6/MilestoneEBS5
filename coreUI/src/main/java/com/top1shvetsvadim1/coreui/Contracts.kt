package com.top1shvetsvadim1.coreui

import androidx.navigation.NavController

interface FeatureAuthContract {
    fun show(navController: NavController)
}

interface FeatureMainContract {
    fun show(navController: NavController)
}